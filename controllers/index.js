const {
  getAll,
  createContact,
  removeContact,
  getByQuery,
} = require("./contacts");

const {
  getAllNote,
  createNote,
  removeNote,
} = require("./note");

module.exports = {
  getAll,
  createContact,
  removeContact,
  getByQuery,
  getAllNote,
  createNote,
  removeNote,
};
